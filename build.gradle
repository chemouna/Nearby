subprojects {
  buildscript {
    repositories {
      mavenCentral()
    }
  }

  repositories {
    mavenCentral()
  }

}

apply from: rootProject.file('dependencies.gradle')

task clean(type: Delete) {
  delete rootProject.buildDir
}
